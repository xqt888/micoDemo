FROM nginx:latest
##添加配置文件
MAINTAINER bmsoft
ARG BUILD_PATH
ARG FILE_PATH
ARG NGINXFILE
ENV NGINXFILE=${NGINXFILE}
###添加文件
ENV BUILD_PATH=${BUILD_PATH}
ENV FILE_PATH=${FILE_PATH}
ADD $BUILD_PATH $FILE_PATH
ADD $NGINXFILE /etc/nginx/conf.d
